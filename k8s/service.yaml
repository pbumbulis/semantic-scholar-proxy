apiVersion: v1
kind: Service
metadata:
  name: semantic-scholar-proxy
  namespace: semantic-scholar
  labels:
    app: semantic-scholar-proxy
spec:
  selector:
    app: semantic-scholar-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
