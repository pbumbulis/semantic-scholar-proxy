# Template — do NOT commit actual values.
# Create this secret manually:
#   kubectl create secret generic semantic-scholar-proxy \
#     --namespace semantic-scholar \
#     --from-literal=api-key=YOUR_API_KEY_HERE
apiVersion: v1
kind: Secret
metadata:
  name: semantic-scholar-proxy
  namespace: semantic-scholar
type: Opaque
stringData:
  api-key: "REPLACE_ME"
